// import { selectPersonalDetailDomain } from '../selectors';

describe('selectPersonalDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
